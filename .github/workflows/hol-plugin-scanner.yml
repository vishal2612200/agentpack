name: HOL Plugin Scanner

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  security-events: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: HOL Plugin Scanner
        uses: hashgraph-online/ai-plugin-scanner-action@b4ca70a7342b42e712df83f832b464110cb81f18
        with:
          plugin_dir: "src/agentpack/data/codex_plugin"
          mode: scan
          min_score: 80
          fail_on_severity: high
          format: sarif
          upload_sarif: true
