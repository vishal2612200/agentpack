# Semantic graph fixtures

Each language directory is a deterministic source fixture and its expected
relationship manifest. `fixture_version` changes when the expected contract
changes. Benchmark consumers must report missing ground truth as unavailable.
